func main() {
	n := 0
	if true {
		n := 1
		n++
		fmt.Println(n) // 2
	}
	fmt.Println(n) // 0
}

Область видимости
В блоке if мы инициализируем локальную переменную n никак не влияющую на переменную n блока main